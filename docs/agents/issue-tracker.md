# Issue tracker

Tracker: GitHub Issues, via the `gh` CLI.

Status: no GitHub remote is configured yet for this repo. Once the GitHub repo
exists, add it as `origin` (e.g. `git remote add origin git@github.com:<owner>/<repo>.git`)
and update this file to record the `owner/repo` path.

Until then, skills that read/write issues should ask before assuming a target repo.
